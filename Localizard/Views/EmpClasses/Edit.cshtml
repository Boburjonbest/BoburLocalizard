@model Localizard.Models.EmpClass

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>EmpClass</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Name" />
            <div class="form-group">
                <label asp-for="DefaultLanguage" class="control-label"></label>
                <input asp-for="DefaultLanguage" class="form-control" />
                <span asp-validation-for="DefaultLanguage" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AvailableLanguage" class="control-label"></label>
                <input asp-for="AvailableLanguage" class="form-control" />
                <span asp-validation-for="AvailableLanguage" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
